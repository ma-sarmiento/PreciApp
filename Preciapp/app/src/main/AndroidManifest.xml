<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.proyecto_movil">

    <!-- 🔐 PERMISOS -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 📸 FEATURES opcionales -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Proyecto_movil"
        tools:targetApi="31">

        <!-- 🔑 Clave de Google Maps -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <!-- 📂 FileProvider para la cámara -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- 🚀 Activity principal (launcher) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 📱 Otras activities del proyecto -->
        <activity android:name=".RegisterActivity" />
        <activity android:name=".Principal" />
        <activity android:name=".CameraListActivity" />
        <activity android:name=".ListaProductosActivity" />
        <activity android:name=".DefinirPresupuestoActivity" />
        <activity android:name=".RutaEficienteActivity" />
        <activity android:name=".CompararPreciosActivity" />
        <activity android:name=".VerPerfilActivity" />
        <activity android:name=".NotificacionesActivity" />
        <activity android:name=".EditarCuentaActivity" />
        <activity android:name=".ManualProductActivity" />
        <activity android:name=".ComparacionActivity" />
        <activity
            android:name=".MapPickerActivity"
            android:exported="false" />
        <activity
            android:name=".MapaRutaActivity"
            android:exported="false" />


    </application>

</manifest>

